import React from 'react';

const Card = (props) => {
	return (
		<div>
		
			<h1>Time {/*props.sevenHour.now.time*/}</h1>
			<div>Temp {/*props.sevenHour.now.temp*/}</div>
			<h3>Condition {/*props.sevenHour.now.condition*/}</h3>
		
		</div>

	)
}





export default Card;